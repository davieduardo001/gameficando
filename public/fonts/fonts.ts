// fonts.js
import { Lato, Nova_Square } from 'next/font/google';

export const lato = Lato({
  weight: ['400', '700'],
  subsets: ['latin'],
});

export const nova_square = Nova_Square({
  weight: '400',
  subsets: ['latin'],
});
