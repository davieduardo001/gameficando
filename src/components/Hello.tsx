import { nova_square, lato } from "../../public/fonts/fonts";


export default function Hello() {
    return (
      <h2 className={`${nova_square.className} text-2xl font-bold`}>Hello from a component!</h2>
    );
  }
  